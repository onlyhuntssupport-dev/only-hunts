import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function StaffPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Staff Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>A list of staff members and their roles will appear here.</p>
      </CardContent>
    </Card>
  )
}

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function VerificationsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Verifications</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Outfitters waiting for verification will appear here.</p>
      </CardContent>
    </Card>
  )
}

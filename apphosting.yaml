# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1

  env:
  - variable: FIREBASE_PROJECT_ID
    secret: FIREBASE_PROJECT_ID
  - variable: FIREBASE_CLIENT_EMAIL
    secret: FIREBASE_CLIENT_EMAIL
  - variable: FIREBASE_PRIVATE_KEY
    secret: FIREBASE_PRIVATE_KEY
  - variable: RESEND_API_KEY
    secret: RESEND_API_KEY
The Exact Fix for src/app/outfitter/dashboard/leads/page.tsx
Open the file src/app/outfitter/dashboard/leads/page.tsx. Scroll down to where your return statement is, specifically where the <TableBody> starts.

Replace your entire <TableBody> ... </TableBody> section with this exact block of code: